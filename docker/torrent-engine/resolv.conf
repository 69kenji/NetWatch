# NetWatch VPN DNS. Backend, Prowlarr and libtorrent all mount this resolver.
# It is reachable through wg0 and avoids Docker's embedded 127.0.0.11 resolver,
# so DNS lookups follow the same fail-closed VPN namespace as application traffic.
nameserver 100.64.0.63
options timeout:2 attempts:2
